<div class="container-fluid">
  
  <!-- Headings -->
  <div class="col-md-offset-1 col-md-10 headings">
  <h1 style="margin-top: 0px; margin-bottom: 50px;">CODING WITH CLAIR</h1>
  <p class="opening-paragraph-<%= params[:controller]%>">Hello and welcome to my Blog/Portfolio! I'm currently teaching myself to code and...well...its tough. This blog is my place to vent, question and generally comment on what I'm learning. If you are able to help answer any of the questions, please add your suggestions in the comments. If you are learning to code then I have some guides full of things that I've learnt and, of course, feel free to check out my portfolio!</p>
  </div>

  
  <!-- ADMIN -->
       <% if user_signed_in? %>
      <%= link_to "Add new post", new_post_path, class: "btn btn-default" %>
      <% end %>

  
  <!-- Main body -->
  <div class="row">
    <div class="col-md-offset-1 col-md-8">
     
      <% @posts.each do |post| %>
      <div class="individualpost">
        
        
        <div class="row">
          <div class="col-md-3" style="padding-top:20px;">
						<p style="text-align: center;"><%= image_tag post.image.url(:thumb), class:"img-responsive image"%> </p>
          </div>
          <div class="col-md-8">
						<p><strong style="font-size: large;"><%= link_to post.title, post_path(post) %></strong> &nbsp; &nbsp;  Posted on  <%= post.created_at.to_date %></p>
          <p><%= sanitize(post.content.truncate(600)) %></p>
          </div>
        </div><!-- row -->
        
        <!-- ADMIN -->
        <% if user_signed_in? %>
          <%= link_to "Edit", edit_post_path(post), class:"btn btn-default" %><br>
        <% end %>
        
       </div><!-- individual post -->
       <div class="postseperator">
       </div>
      
      <% end %> <!-- @posts iteration -->
			
      <%= will_paginate @collection, renderer: BootstrapPagination::Rails %>
				
   </div> <!-- col-md -->
   

    <!-- Side navigation -->
    <%= render partial: 'layouts/sidenav' %>
    
  </div><!-- row --> 
 
</div><!-- container -->
