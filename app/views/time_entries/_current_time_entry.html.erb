<div class="row">
  <% if live_post != nil %>
    <p class="opening-paragraph-<%= params[:controller]%>", id="live_project_link">
     <%="Right now (#{Time.now.strftime("%H:%M")}) I am working on #{live_post["description"]}"%>
  <% end %>
</div>

